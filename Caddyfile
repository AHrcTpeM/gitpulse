gitpulse.duckdns.org {
    # Proxy all requests to the Node.js application
    reverse_proxy app:3000

    # Protect Prometheus with basic authentication
    # User: admin, Password: GitPulse2026!
    basicauth /prometheus* {
        admin JDJhJDEyJE45cW84dUxPaWNrZ3gyWk1SWm9NeWVJakpBZ05vM0dEdS42ZlhUbzU2V2s4ODh5OGhTM0t5
    }
    
    # Proxy to Prometheus UI
    handle /prometheus* {
        reverse_proxy prometheus:9090
    }

    # Enable access logging
    log {
        output file /data/access.log
    }
}
